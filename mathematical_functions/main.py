import numpy as np

# Universal Functions (ufuncs)
arr = np.array([1, 4, 9, 16, 25])

# Basic math
print(np.sqrt(arr))
print(np.square(arr))
print(np.abs([-4, -2, 4, -4.6, 5.6]))
print(np.power(arr, 3))

# Rounding